const menu=document.getElementById("menu"),links=document.getElementById("links");menu.addEventListener("click",()=>links.classList.toggle("open"));links.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>links.classList.remove("open")));
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("visible");io.unobserve(e.target)}}),{threshold:.12});document.querySelectorAll(".reveal").forEach(el=>io.observe(el));
const target=new Date("2026-09-24T17:00:00+10:00").getTime(),pad=n=>String(n).padStart(2,"0");function tick(){let d=Math.max(0,target-Date.now()),days=Math.floor(d/86400000);d%=86400000;let hours=Math.floor(d/3600000);d%=3600000;let minutes=Math.floor(d/60000);d%=60000;let seconds=Math.floor(d/1000);document.getElementById("days").textContent=pad(days);document.getElementById("hours").textContent=pad(hours);document.getElementById("minutes").textContent=pad(minutes);document.getElementById("seconds").textContent=pad(seconds)}tick();setInterval(tick,1000);
const lb=document.getElementById("lightbox"),lbi=document.getElementById("lightboxImg");document.querySelectorAll(".shot").forEach(b=>b.addEventListener("click",()=>{lbi.src=b.dataset.img;lb.classList.add("open")}));document.getElementById("close").addEventListener("click",()=>lb.classList.remove("open"));lb.addEventListener("click",e=>{if(e.target===lb)lb.classList.remove("open")});document.addEventListener("keydown",e=>{if(e.key==="Escape")lb.classList.remove("open")});
document.getElementById("rsvpForm").addEventListener("submit",async e=>{e.preventDefault();const name=document.getElementById("guestName").value.trim(),attendance=document.getElementById("attendance").value,guests=document.getElementById("guests").value,message=document.getElementById("message").value.trim();const text="RSVP for Bisesh & Debansi — 24 September 2026\n\nName(s): "+name+"\nAttendance: "+attendance+"\nGuests: "+guests+"\nMessage: "+(message||"—");const status=document.getElementById("status");try{if(navigator.share){await navigator.share({title:"Bisesh & Debansi RSVP",text});status.textContent="RSVP message opened in your share sheet ♡"}else{await navigator.clipboard.writeText(text);status.textContent="RSVP message copied — send it to Bisesh or Debansi ♡"}}catch(err){status.textContent="Your RSVP is ready. You can copy the details above and send them to us ♡"}});